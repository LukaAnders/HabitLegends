import{motion}from'framer-motion';import{Check,Coins,Gift,LockKeyhole,Sparkles}from'lucide-react'
export function DailyChest({completed,total,claimed,busy,onClaim}:{completed:number;total:number;claimed:boolean;busy:boolean;onClaim:()=>void}){const progress=total?Math.min(100,completed/total*100):0,available=total>0&&completed>=total&&!claimed;return <motion.section whileHover={{y:-3}} className={`daily-chest ${available?'available':''}`}><div className="chest-rays"/><div className="relative z-10 flex items-center gap-5"><motion.div animate={available?{y:[0,-5,0],rotate:[0,-2,2,0]}:{y:[0,-2,0]}} transition={{duration:available?1.8:4,repeat:Infinity}} className="chest-illustration"><div className="chest-lid"/><div className="chest-body">{claimed?<Check size={19}/>:<LockKeyhole size={17}/>}</div></motion.div><div className="min-w-0 flex-1"><p className="text-[10px] font-bold uppercase tracking-[.25em] text-gold">Baú do aventureiro</p><h3 className="mt-1 font-display text-lg font-bold text-parchment">Recompensa diária</h3><p className="mt-1 text-xs text-mist">{claimed?'Recompensa resgatada hoje':`Complete ${total} missões para abrir`}</p></div></div><div className="relative z-10 mt-5"><div className="mb-2 flex justify-between text-xs"><span className="text-mist">Progresso: {completed} de {total}</span><strong className="text-gold">{Math.round(progress)}%</strong></div><div className="h-2 overflow-hidden rounded-full bg-black/40"><motion.div animate={{width:`${progress}%`}} className="h-full rounded-full bg-gradient-to-r from-amber-700 to-gold"/></div><div className="mt-4 flex flex-wrap gap-3 text-xs"><span className="reward-chip text-purple-200"><Sparkles size={13}/>+200 XP</span><span className="reward-chip text-amber-200"><Coins size={13}/>+100</span><span className="reward-chip text-blue-200"><Gift size={13}/>{claimed?'Baú aberto':'Recompensa diária'}</span></div><button className="daily-claim-button" disabled={!available||busy} onClick={onClaim}>{busy?'Abrindo...':claimed?'Resgatado':available?'Abrir baú':'Complete as missões'}</button></div></motion.section>}
export const DailyProgress=DailyChest
