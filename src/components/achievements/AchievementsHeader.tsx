import { Award, Trophy } from 'lucide-react'
export function AchievementsHeader({ unlocked, total }: { unlocked: number; total: number }) { const percent = total ? Math.round(unlocked / total * 100) : 0; return <header className="achievements-hero"><div><span><Award size={15} /> Salão da glória</span><h1>Conquistas</h1><p>Cada feito grava seu nome nas crônicas do reino.</p></div><div className="achievement-summary"><Trophy size={28} /><span><small>Medalhas conquistadas</small><strong>{unlocked} <i>de {total}</i></strong></span><b>{percent}%</b><div><i style={{ width: `${percent}%` }} /></div></div></header> }
