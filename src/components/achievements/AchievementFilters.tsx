import type { AchievementCategory } from '../../types/achievements'
import { achievementCategoryLabels } from '../../utils/achievementUtils'
export type AchievementStateFilter = 'all' | 'progress' | 'unlocked' | 'available' | 'completed'
export function AchievementFilters({ category, state, onCategory, onState }: { category: 'all' | AchievementCategory; state: AchievementStateFilter; onCategory: (value: 'all' | AchievementCategory) => void; onState: (value: AchievementStateFilter) => void }) { const categories: Array<'all' | AchievementCategory> = ['all', 'missions', 'consistency', 'progression', 'market', 'collection', 'journey', 'secret']; return <div className="achievement-filters"><div>{categories.map(item => <button className={category === item ? 'active' : ''} key={item} onClick={() => onCategory(item)}>{item === 'all' ? 'Todas' : achievementCategoryLabels[item]}</button>)}</div><select value={state} onChange={event => onState(event.target.value as AchievementStateFilter)}><option value="all">Todos os estados</option><option value="progress">Em progresso</option><option value="unlocked">Desbloqueadas</option><option value="available">Recompensas disponíveis</option><option value="completed">Concluídas</option></select></div> }
