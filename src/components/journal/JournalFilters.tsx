import { Search } from 'lucide-react';import type{JournalFilter}from'../../utils/journalUtils'
const options:[JournalFilter,string][]=[['all','Todos'],['missions','Missões'],['market','Mercado'],['achievements','Conquistas'],['journey','Jornada'],['levels','Níveis'],['equipment','Equipamentos'],['streak','Streak']]
export function JournalFilters({filter,search,onFilter,onSearch}:{filter:JournalFilter;search:string;onFilter:(v:JournalFilter)=>void;onSearch:(v:string)=>void}){return <div className="journal-filters"><div>{options.map(([value,label])=><button key={value} className={filter===value?'active':''} onClick={()=>onFilter(value)}>{label}</button>)}</div><label><Search/><input value={search} onChange={e=>onSearch(e.target.value)} placeholder="Pesquisar nas crônicas..."/></label></div>}
