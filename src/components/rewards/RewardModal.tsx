import { AnimatePresence, motion } from 'framer-motion'
import { Coins, Flame, Sparkles, Trophy, X } from 'lucide-react'
import type { CompletionReward } from '../../types/completions'

export function RewardModal({ reward, onClose }: { reward: CompletionReward | null; onClose: () => void }) { const progress = reward ? reward.currentXp / reward.requiredXp * 100 : 0; return <AnimatePresence>{reward && <motion.div className="game-modal-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.section role="dialog" aria-modal="true" aria-labelledby="reward-title" initial={{ scale: .75, opacity: 0, y: 30 }} animate={{ scale: 1, opacity: 1, y: 0 }} exit={{ scale: .9, opacity: 0 }} className="reward-modal"><div className="reward-burst" /><button onClick={onClose} className="modal-close" aria-label="Fechar"><X /></button><motion.div animate={{ rotate: [0, -8, 8, 0], y: [0, -5, 0] }} transition={{ duration: 1.5 }} className="reward-emblem"><Trophy size={38} /></motion.div><span className="eyebrow justify-center">Missão concluída</span><h2 id="reward-title">{reward.taskTitle}</h2><p>Seu esforço fortaleceu a lenda.</p><div className="reward-gains"><motion.div initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ delay: .2 }}><Sparkles /><span>Experiência</span><strong>+{reward.xpEarned} XP</strong></motion.div><motion.div initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ delay: .3 }}><Coins /><span>Ouro</span><strong>+{reward.goldEarned}</strong></motion.div><motion.div initial={{ scale: 0 }} animate={{ scale: 1 }} transition={{ delay: .4 }}><Flame /><span>Sequência</span><strong>{reward.streak} dias</strong></motion.div></div>{reward.levelBonusGold > 0 && <div className="level-bonus">Bônus de nível: +{reward.levelBonusGold} ouro</div>}<div className="reward-balance"><span>Novo saldo</span><strong><Coins size={15} />{reward.newGold}</strong></div><div className="reward-progress"><div><span>Nível {reward.newLevel}</span><span>{reward.currentXp} / {reward.requiredXp} XP</span></div><div><motion.i initial={{ width: 0 }} animate={{ width: `${progress}%` }} /></div></div><button onClick={onClose} className="auth-submit">Receber recompensa</button></motion.section></motion.div>}</AnimatePresence> }
