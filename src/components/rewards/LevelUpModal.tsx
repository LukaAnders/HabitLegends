import { AnimatePresence, motion } from 'framer-motion'
import { Crown, Sparkles } from 'lucide-react'

export function LevelUpModal({ level, levelsGained, open, onClose }: { level: number; levelsGained: number; open: boolean; onClose: () => void }) { return <AnimatePresence>{open && <motion.div className="game-modal-backdrop levelup-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.section role="dialog" aria-modal="true" className="levelup-modal" initial={{ scale: .5, rotate: -5, opacity: 0 }} animate={{ scale: 1, rotate: 0, opacity: 1 }}><div className="levelup-rays" /><motion.div animate={{ y: [0, -8, 0], filter: ['brightness(1)', 'brightness(1.4)', 'brightness(1)'] }} transition={{ repeat: Infinity, duration: 2 }}><Crown size={52} /></motion.div><span><Sparkles size={14} />Ascensão</span><h2>Novo Nível!</h2><strong>{level}</strong><p>{levelsGained > 1 ? `${levelsGained} níveis conquistados de uma só vez.` : 'Sua dedicação elevou seu poder.'}</p><p className="text-gold">+{levelsGained * 50} ouro de ascensão</p><button onClick={onClose} className="primary-game-button">Continuar jornada</button></motion.section></motion.div>}</AnimatePresence> }
