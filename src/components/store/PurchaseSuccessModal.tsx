import { AnimatePresence, motion } from 'framer-motion'
import { Check, Coins, Package, Sparkles, X } from 'lucide-react'
import { Link } from 'react-router-dom'
import type { PurchaseResult } from '../../types/store'

export function PurchaseSuccessModal({ purchase, onClose }: { purchase: PurchaseResult | null; onClose: () => void }) { return <AnimatePresence>{purchase && <motion.div className="game-modal-backdrop" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><motion.section role="dialog" aria-modal="true" className="purchase-modal" initial={{ scale: .8, opacity: 0, y: 25 }} animate={{ scale: 1, opacity: 1, y: 0 }}><button onClick={onClose} className="modal-close"><X /></button><motion.div animate={{ y: [0, -6, 0] }} transition={{ repeat: Infinity, duration: 2.5 }} className="purchase-item-image"><div style={{ background: `radial-gradient(circle, var(--purchase-color, #F2B84B), transparent 68%)` }} /><img src={purchase.item.imageUrl} alt={purchase.item.name} /></motion.div><span className="eyebrow justify-center"><Sparkles size={13} />Negócio concluído</span><h2>{purchase.item.name}</h2><p>O artefato foi enviado para seu inventário.</p><div className="purchase-balance"><span><Check size={15} />Item adquirido</span><strong><Coins size={15} />Saldo: {purchase.newGold}</strong></div><Link to="/inventario" onClick={onClose} className="auth-submit"><Package size={17} />Abrir inventário</Link></motion.section></motion.div>}</AnimatePresence> }
