import { MessageCircle, ShoppingBag } from 'lucide-react'
import { Link } from 'react-router-dom'
import { ITEM_RARITY_LABELS } from '../constants/store'
import { useStoreItems } from '../hooks/useStoreItems'

export function MerchantPreview() { const { items, loading } = useStoreItems(); const featured = items.filter(item => item.isAvailable).slice(0, 3); return <section className="merchant-panel"><div className="merchant-intro"><div className="merchant-avatar"><div className="merchant-hood"><span>◉</span><span>◉</span></div><ShoppingBag className="absolute -bottom-1 -right-2 rounded-full border border-gold/30 bg-[#15101c] p-2 text-gold" size={37} /></div><div><div className="flex items-center gap-2 text-gold"><MessageCircle size={15} /><span className="eyebrow">Mercador errante</span></div><h2 className="section-heading mt-1">Artefatos do reino</h2><p className="mt-2 max-w-sm text-sm leading-relaxed text-mist">“Saudações, aventureiro. Novos artefatos chegaram ao reino.”</p><Link to="/mercado" className="mt-4 inline-block text-xs font-bold uppercase tracking-wider text-gold">Visitar o mercado →</Link></div></div><div className="grid min-w-0 flex-1 grid-cols-3 gap-3">{loading ? Array.from({ length: 3 }, (_, index) => <div key={index} className="store-skeleton h-40" />) : featured.map(item => <Link to="/mercado" key={item.id} className={`store-item border rarity-${item.rarity}`}><div className="item-pedestal"><div className="item-aura" /><img src={item.imageUrl} alt="" className="relative z-10 h-16 w-16 object-contain" /></div><span className="text-[9px] uppercase tracking-wider text-mist">{ITEM_RARITY_LABELS[item.rarity]}</span><h3 className="mt-1 truncate font-display text-sm font-bold text-parchment">{item.name}</h3><p className="mt-2 text-xs font-bold text-gold">● {item.price}</p></Link>)}</div></section> }
