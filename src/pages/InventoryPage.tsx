import { Package } from 'lucide-react'
import { ITEM_CATEGORY_LABELS, ITEM_RARITY_LABELS } from '../constants/store'
import { useInventory } from '../hooks/useInventory'

export function InventoryPage() { const { items, loading, error } = useInventory(); return <main className="inventory-page"><header><span className="eyebrow"><Package size={15} />Tesouro pessoal</span><h1>Inventário</h1><p>Artefatos conquistados e adquiridos durante sua jornada.</p></header>{error && <div className="tasks-error">{error}</div>}{loading ? <div className="store-grid"><div className="store-skeleton" /><div className="store-skeleton" /></div> : items.length ? <section className="inventory-grid">{items.map(item => <article key={item.id} className={`inventory-card rarity-${item.rarity}`}><div><img src={item.imageUrl} alt={item.name} /></div><span>{ITEM_RARITY_LABELS[item.rarity]} · {ITEM_CATEGORY_LABELS[item.category]}</span><h2>{item.name}</h2><p>Adquirido por {item.purchasedPrice} ouro</p><button disabled>Equipar na Fase 5</button></article>)}</section> : <section className="tasks-empty"><Package size={38} className="text-gold" /><h2>Seu inventário está vazio</h2><p>Visite o mercado para encontrar seus primeiros artefatos.</p></section>}</main> }
